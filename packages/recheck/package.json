{
  "name": "recheck",
  "description": "A vulnerable RegExp (ReDoS) checker for JavaScript (ECMA-262) RegExp.",
  "license": "MIT",
  "author": "TSUYUSATO Kitsune <make.just.on@gmail.com>",
  "homepage": "https://makenowjust-labo.github.io/recheck/",
  "repository": "https://github.com/MakeNowJust-Labo/recheck.git",
  "version": "4.0.2",
  "main": "main.js",
  "browser": "browser.js",
  "types": "index.d.ts",
  "keywords": [
    "redos",
    "regexp",
    "security",
    "vulnerability"
  ],
  "scripts": {
    "build": "esbuild --bundle --platform=node --format=cjs src/index.js --outfile=main.js && esbuild --bundle --platform=browser --format=esm src/fallback.js --outfile=browser.js"
  },
  "files": [
    "browser.js",
    "main.js"
  ],
  "optionalDependencies": {
    "recheck-macos-x64": "4.0.2",
    "recheck-linux-x64": "4.0.2",
    "recheck-windows-x64": "4.0.2"
  },
  "devDependencies": {
    "esbuild": "^0.13.15"
  }
}
